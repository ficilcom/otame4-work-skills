# Contributing

このリポジトリのスキルは、就職活動と転職で繰り返し発生する作業を、再利用できる手順に変えるためのものである。一般論のアドバイスではなく、判断を変える情報、失敗を防ぐ制約、使える成果物を持たせる。利用者は求職者本人であり、`employer` カテゴリでは求人企業の採用担当者・現場のマネージャー・経営者を対象とする。スキルは公開され、立場ごとに各自の環境にインストールされる。

## スキルを作る

```bash
python3 scripts/new_skill.py <category> <skill-name>
```

| カテゴリ | 範囲 |
| --- | --- |
| `career` | 自己分析、就活・転職の軸、キャリア設計、転職するかどうかの意思決定 |
| `documents` | エントリーシート、履歴書、職務経歴書、ポートフォリオの作成と添削 |
| `interview` | 面接・面談対策、想定質問、逆質問、振り返り |
| `research` | 業界・企業研究、求人票の読み解き、応募先の選定 |
| `offer` | 内定・オファーの比較、条件確認、退職と入社の準備 |
| `trial` | 求職者のおためし転職。応募、条件調整、無償体験と有償業務の準備、体験後の判断 |
| `employer` | 求人企業のおためし転職。業務設計、募集文、社内説明、応募者対応、業務委託の条件明示、振り返り |
| `sidework` | 本業と並行する副業の可否・申請、業務委託で受ける案件の取引条件と報酬の確認 |

`SKILL.md` のフロントマターは最低限これを満たす。

```yaml
---
name: example-skill
description: What the skill does, and when an agent should use it.
license: MIT
---
```

`description` は発見用のメタデータなので、関連する依頼で確実に起動し、無関係な依頼を拾わない程度に具体的にする。`description` は英語、本文と参照資料は日本語で書く。`license` は `MIT` か省略のいずれか。

雛形を埋めたら `.claude-plugin/marketplace.json` にスキルを追加する。カテゴリ初のスキルなら、そのカテゴリのプラグイン項目ごと追加する。

追加・改名・削除時は、READMEの一覧と `skills.sh.json` の表示グループも更新する。`python3 scripts/validate_skills.py` が3箇所すべてを実体と突き合わせるので、更新漏れはそこで落ちる。`description` にコロンと空白を含める場合は値全体を引用符で囲む。公開前に `DISABLE_TELEMETRY=1 npx skills add . --list` で全スキルが読み飛ばされずに表示されることを確認する。このコマンドはインストールしない。

skills.shの表示設定は、デフォルトブランチへの反映後、テレメトリ有効の実際のCLIインストールを通じて取得される。設定ファイルの追加やページ閲覧だけで掲載・即時更新されるわけではない。詳細は [表示設定](https://skills.sh/docs/customize) と [掲載のFAQ](https://skills.sh/docs/faq) を参照する。

## 執筆の原則

- エージェントは有能である前提で書く。判断を変える情報、現実的な失敗を防ぐ制約、非自明な文脈だけを入れる。
- 利用者の目的、制約、決定権を保つ。依頼されていない作業に広げない。外部への行為を勝手に承認しない。
- 入力、前提、判断基準、成果物を明示する。
- `sidework` は、勤務先の規程の確認と案件条件の確認までを行い、可否の結論を出さない。適法性を判定せず、勤務先に知られないための手順や、申告・届出を避ける方法を作らない。
- `trial` と `employer` は単独で使える相談・原稿作成・運用準備として作り、外部操作は本人に残す。未経験者への無償体験は見学・学習・模擬課題を候補にし、企業の実務は経験にかかわらず有償枠を提案する。有償業務では作業量、実働時間と実施期間、報酬、支援、変更条件を明示する。`employer` では、成果への満足や採用判断を支払いの条件にせず、職務と無関係な属性で応募者を選別しない。
- `SKILL.md` は簡潔に保ち、条件分岐は `references/`、決定的な計算は `scripts/`、出力雛形は `assets/` に置く。同じ内容を両方に書かない。
- スクリプトでは入力検証とCLIの定型を書き直さず、同梱の `_common.py` から使う（`require_object`、`require_text`、`optional_date`、`flag_collector`、`run_cli` など）。何を欠落とみなすか、どの条件を注意として出すかはスキル固有の判断なので、各スクリプトに残す。
- 前後の空白に意味がある入力（応募書類の本文など）には `require_text` ではなく `require_raw_text` を使う。`require_text` は strip するため、提出時の文字数と判定がずれる。

## キャリア支援としての品質

このリポジトリのスキルは、人の進路と収入に影響する。次を守る。

- 利用者が示した事実、本人の申告、推定、不明を区別する。`unknown` を 0 や達成に置き換えない。
- 経験、実績、資格、数値を創作しない。原稿と本人の発言にないものを提案として書かない。
- 合否、通過率、内定確率を予測しない。
- 給与相場、労働条件、制度、法令、企業情報など時点に依存する事実は一次情報で確認し、確認できないものは未確認と書く。
- 労働法、税務、社会保険の判断を専門的な確定判断として示さない。争いのある論点は専門家への相談を選択肢として残す。
- 応募の送信、企業への連絡、退職の意思表示など後戻りしにくい行為の直前で、必ず承認を求める。
- 作業に必要のない個人情報を集めない。利用者が明示的に求めない限り、原稿や添削結果をファイルに残さない。
- 実在する求職者のデータを公開リポジトリにコミットしない。例示は明らかに架空の内容にする。

## 意思決定の品質

重い判断（転職するか、内定を受けるか、どの企業に応募するか）を支えるスキルでは、文字数ではなく次を基準にする。

- 軽量な中核分析と、詳細な追加入力を分ける。全項目を要求して着手を止めない。
- 結論を反転させうる前提が1つでもあるなら、シナリオか感度を比較する。
- 経済的な比較（年収、生活費、勤務地）と、要件のゲート（資格、ビザ、勤務条件）と、定性的なリスクを混ぜない。
- 提案には、根拠、反証、検証手段、やめる条件、期限を添える。

## 検証

```bash
python3 scripts/validate_skills.py
python3 scripts/run_tests.py
```

決定的なスクリプトのテストは `tests/<category>/<skill-name>/` に置く。インストール対象のスキルディレクトリ内には置かない（検証スクリプトが落とす）。

テストからスクリプトを読むときは `tests/_loader.py` を使う。

```python
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[2]))

from _loader import load_script, run_script, script_path  # noqa: E402

SCRIPT_PATH = "skills/<category>/<skill-name>/scripts/<name>.py"
SCRIPT = script_path(SCRIPT_PATH)
MODULE = load_script(SCRIPT_PATH)
```

`importlib` を直接使うとスクリプトのディレクトリが `sys.path` に載らず、同梱の `_common.py` を import できない。CLIとしての挙動は `run_script(SCRIPT, payload)` で確かめる。

`_common.py` を変更したいときは `scripts/_common_source.py` を編集し、`python3 scripts/sync_common.py` で各スキルへ配り直してからコミットする。

## レビュー時の確認

- 求職者、または `employer` の求人企業の具体的で反復する作業を解いている。
- 粒度が既存スキルと揃っている。1スキル1作業が既定で、複数段階を束ねる場合は AGENTS.md の条件を満たし、`## 進め方` が依頼と参照の対応表になっている。
- 名前と `description` から起動条件が予測できる。
- 利用者の意図と決定権を保っている。
- 具体的で使える成果物が出る。
- 出典、計算、不確実性の扱いが適切である。
- 個人情報が混入していない。
- 検証スクリプトが通る。
